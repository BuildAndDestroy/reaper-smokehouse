# EXAMPLE ONLY — do not put real passwords here and commit.
# Generate the real Secret locally (gitignored):
#   export MONGO_ROOT_PASSWORD='...'
#   ./scripts/k8s-gen-mongodb-secret.sh
# Or: kubectl create secret generic mongodb-credentials ...
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-credentials
  labels:
    app: mongodb
stringData:
  mongo-root-username: admin
  mongo-root-password: REPLACE_WITH_GENERATED_OR_ENV_VALUE
